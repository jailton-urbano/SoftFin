@model SoftFin.Web.Models.FolhaPagamentoTipo
@{
    ViewBag.Title = "Tipo de Folha de Pagamento";

}



<table id="jqgFolhaPagamentoTipo" cellpadding="0" cellspacing="0"></table>
<div   id="jqgFolhaPagamentoTipoOpc" style="text-align:center;"></div>

<script type="text/javascript">
    $(document).ready(function () {

        Metronic.init(); // init metronic core components
        Layout.init(); // init current layout
        QuickSidebar.init(); // init quick sidebar

        $('#jqgFolhaPagamentoTipo').jqGrid({
            //url from wich data should be requested
            url: '@Url.Action("Listas")',
            //type of data
            datatype: 'json',
            //url access method type
            mtype: 'POST',
            //columns names
            colNames: ['Descrição'],
            //columns model
            colModel: [
                        { name: 'txtdescricao', index: 'Tipo', align: 'left', editable: false, width: '625', hidden: false }


            ],
            //pager for grid
            pager: $('#jqgFolhaPagamentoTipoOpc'),
            //number of rows per page
            rowNum: 12,
            //initial sorting column
            sortname: 'Tipo',
            //initial sorting direction
            sortorder: 'asc',
            viewrecords: true,
            //grid height
            height: '100%'

        });

        $('#jqgFolhaPagamentoTipo').jqGrid('navGrid', '#jqgFolhaPagamentoTipoOpc',
            { add: false, del: false, edit: false, search: false });

        $('#jqgFolhaPagamentoTipo').navButtonAdd('#jqgFolhaPagamentoTipoOpc', { // custom edit button
            caption: "Novo",
            buttonicon: "ui-icon ui-icon-plus",
            onClickButton: function () {
                window.location = "Create/";
            },
            position: "Last"
        });

        $('#jqgFolhaPagamentoTipo').navButtonAdd('#jqgFolhaPagamentoTipoOpc', { // custom edit button
            caption: "Alterar",
            buttonicon: "ui-icon ui-icon-pencil",
            onClickButton: function () {
                var gr = jQuery("#jqgFolhaPagamentoTipo").jqGrid('getGridParam', 'selrow');
                if (gr != null) {
                    window.location = "Edit/" + gr;
                }
                else {
                    alert("Selecione 1 coluna.");
                };
            },
            position: "Last"
        });

        $('#jqgFolhaPagamentoTipo').navButtonAdd('#jqgFolhaPagamentoTipoOpc', { // custom edit button
            caption: "Excluir",
            buttonicon: "ui-icon ui-icon-trash",
            onClickButton: function () {
                var gr = jQuery("#jqgFolhaPagamentoTipo").jqGrid('getGridParam', 'selrow');
                if (gr != null) {
                    window.location = "Delete/" + gr;
                }
                else {
                    alert("Selecione 1 coluna.");
                };
            },
            position: "Last"
        });

    });

    </script>


